# WSL fixtures

No WSL fixtures were captured. On 2026-07-18, `wsl.exe -l -v` showed only stopped `docker-desktop` (version 2); no WSL2 Ubuntu distribution was registered. Ubuntu installation is required before WSL-side Claude Code measurements can proceed.

`agents.json` in this directory is a hand-written sample based on the documented WSL output shape, not a real capture.
